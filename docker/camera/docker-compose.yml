version: '3'
services:
  camera:
    image: huntsmanarray/camera:latest
    user: huntsman
    privileged: true
    network: host
    stdin_open: true
    tty: true
    volumes:
      - "~/.ssh:/home/huntsman/.ssh"
