version: '3.7'
services:
  camera:
    image: huntsmanarray/camera:${TAG:-develop}
    privileged: true
    network_mode: host
    stdin_open: true
    tty: true
    volumes:
      - "${HOME}/.ssh:/home/huntsman/.ssh"
