version: '3'
services:
  camera:
    image: huntsmanarray/camera:tenlens
    build:
      context: .
    user: huntsman
    privileged: true
    network_mode: host
    stdin_open: true
    tty: true
    volumes:
      - "${HOME}/.ssh:/home/huntsman/.ssh"
